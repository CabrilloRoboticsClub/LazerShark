- name: Installing extra packages needed by LazerShark with apt.
  apt:
    name:
      - ros-humble-tf2
      - ros-humble-tf2-py
      - ros-humble-tf2-ros
      - ros-humble-tf2-ros-py
      - docker.io
      - build-essential 
    state: present

- name: Installing Python packages in your home directory with pip.
  become: false
  pip:
    name:
      - setuptools
      - args
      - clint
      - pyftdi
      - pyserial
      - pyusb
      - pip
      - opencv-python
      - simple-pid
      - numpy
      # - git+https://github.com/CabrilloRoboticsClub/naviguider_simpleserial.git
      # - git+https://github.com/CabrilloRoboticsClub/targetpoint_lib.git

# - name: Getting Gazebo packages
#   become: true
#   ansible.builtin.get_url:
#     url: https://packages.osrfoundation.org/gazebo.gpg
#     dest: /usr/share/keyrings/pkgs-osrf-archive-keyring.gpg
#     owner: root
#     group: root
#     mode: 0644
#   when: ros_repos is failed

# - name: Installing Gazebo packages
#   apt:
#     - gz-harmonic
#     - ros-humble-ros-gzharmonic

# - name: Setup Gazebo 
#   hosts: all
#   environment:
#     ENV GZ_VERSION: "harmonic"